markitup-rails (textile)
========================

Gem for the vendor assets for markitup textile. Requires Rails 3.1 or higher.

## Usage

In application.css `*= require markitup` and in application.js `//= require markitup`

